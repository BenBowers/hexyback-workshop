import { components } from '@openapi';

//TODO Work out how to access the types defined in `backend/openapi/event-bridge-schema-types.d.ts`
export type BorrowerProfile = components['schemas']['Borrower']